# ============================================================================
# Calendário Sync - Ficheiro de Configuração de Ambiente
# 
# INSTRUÇÕES:
# 1. Copiar este ficheiro: cp .env.example .env
# 2. Editar .env com as suas configurações
# 3. NUNCA fazer commit do ficheiro .env com dados reais
# 4. Manter .env.example atualizado mas SEM dados sensíveis
#
# VERSÃO: 1.0
# DATA: Janeiro 2026
# ============================================================================

# ============================================================================
# SHORT CONFIGURATION
# ============================================================================

AIRBNB_ICAL_URL=https://www.airbnb.com.br/calendar/ical/123456abc.ics
BOOKING_ICAL_URL=https://ical.booking.com/v1/export/t/abc123456.ics
VRBO_ICAL_URL=http://www.vrbo.com/icalendar/123abc456.ics
BUFFER_DAYS_BEFORE=1
BUFFER_DAYS_AFTER=1
PROPERTY_NAME=StudioAxe
OUTPUT_FILE=master_calendar.ics
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=meu_email@gmail.com
EMAIL_PASSWORD=aaaa bbbb cccc dddd
NOTIFICATION_EMAIL=meu_email@gmail.com
ERROR_EMAIL=meu_email@gmail.com
EMAIL_ON_ERROR=true
EMAIL_ATTACH_LOG=true
LOG_FILE=sync.log
LOG_LEVEL=INFO

# ============================================================================
# FLASK CONFIGURATION
# ============================================================================

# Modo de execução: development, production, testing
FLASK_ENV=development

# Ficheiro principal da aplicação
FLASK_APP=app_fase2.py

# Chave secreta para sessões (gerar com: python -c "import os; print(os.urandom(24).hex())")
# ⚠️ CRÍTICO: Mudar em produção! Usar valor aleatório forte
SECRET_KEY=your-secret-key-here-change-this-in-production

# Debug mode (desabilitar em produção!)
FLASK_DEBUG=1

# ============================================================================
# GITHUB CONFIGURATION
# ============================================================================

# Token de autenticação GitHub (PAT - Personal Access Token)
# Gerar em: https://github.com/settings/tokens
# Permissões necessárias: repo (full), workflow
# ⚠️ CRÍTICO: Manter seguro, não fazer commit
GITHUB_TOKEN=ghp_your_github_token_here

# Proprietário do repositório (username ou organização)
GITHUB_OWNER=seu_usuario_ou_organizacao

# Nome do repositório
GITHUB_REPO=seu_repositorio

# Nome do workflow a disparar
# Exemplo: sync_calendars.yml
GITHUB_WORKFLOW=sync_calendars

# Ramo (branch) a disparar
# Padrão: main
GITHUB_BRANCH=main

# ============================================================================
# AUTENTICAÇÃO DA WEB
# ============================================================================

# Utilizador padrão para login na web
# Usar formato: username:password
# Exemplo: admin:senha123
WEB_USERNAME=admin

# Senha do utilizador web
# ⚠️ CRÍTICO: Usar senha forte em produção
WEB_PASSWORD=change_this_password

# Sessão timeout em minutos
SESSION_TIMEOUT_MINUTES=120

# ============================================================================
# CALENDÁRIO CONFIGURATION
# ============================================================================

# Diretório de dados
DATA_DIR=./data

# Diretório de calendários
CALENDAR_DIR=./data/calendars

# Ficheiro de eventos manuais
MANUAL_EVENTS_FILE=./data/manual_events.json

# Ficheiro de calendário importado
IMPORT_CALENDAR_FILE=./data/import_calendar.ics

# Ficheiro de calendário master (saída)
MASTER_CALENDAR_FILE=./data/master_calendar.ics

# ============================================================================
# SERVIDOR
# ============================================================================

# Host para ouvir
HOST=0.0.0.0

# Porto
PORT=5000

# URL base da aplicação (para links externos)
BASE_URL=http://localhost:5000

# Permitir requisições de outros origins (CORS)
CORS_ORIGINS=*

# ============================================================================
# LOGGING
# ============================================================================

# Nível de logging: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Ficheiro de log
LOG_FILE=./logs/app.log

# Tamanho máximo de log antes de rodar (em bytes)
LOG_MAX_SIZE=10485760  # 10MB

# Número de ficheiros de log a guardar
LOG_BACKUP_COUNT=10

# ============================================================================
# FEATURES E COMPORTAMENTO
# ============================================================================

# Ativar sincronização automática ao iniciar
AUTO_SYNC_ON_STARTUP=false

# Intervalo de sincronização automática (em minutos)
# 0 = desativado
AUTO_SYNC_INTERVAL=0

# Ativar API webhooks
ENABLE_WEBHOOKS=false

# Ativar compressão de resposta
ENABLE_COMPRESSION=true

# ============================================================================
# PROMETHEUS METRICS (Opcional)
# ============================================================================

# Ativar métricas Prometheus
PROMETHEUS_ENABLED=false

# Porto das métricas
PROMETHEUS_PORT=9090

# ============================================================================
# NOTIFICAÇÕES (Futuro)
# ============================================================================

# Email para notificações
NOTIFICATION_EMAIL=seu_email@example.com

# Servidor SMTP
SMTP_SERVER=smtp.gmail.com

# Porto SMTP
SMTP_PORT=587

# Utilizador SMTP
SMTP_USER=seu_email@gmail.com

# Senha SMTP (app-specific password para Gmail)
SMTP_PASSWORD=your_smtp_password

# ============================================================================
# DESENVOLVIMENTO
# ============================================================================

# Modo de teste (desativa segurança, usa dados mock)
TESTING=false

# Verbosidade
VERBOSE=false

# ============================================================================
# NOTAS DE SEGURANÇA
# ============================================================================
#
# 1. SECRETS:
#    - Nunca commitar ficheiro .env com dados reais
#    - Usar variables de ambiente em produção
#    - Rotacionar tokens regularmente
#
# 2. GITHUB TOKEN:
#    - Gerar em https://github.com/settings/tokens
#    - Usar "Fine-grained" tokens quando possível
#    - Definir expiração (90 dias recomendado)
#    - Limitar permissões ao mínimo necessário
#
# 3. PASSWORDS:
#    - Usar mínimo 12 caracteres
#    - Incluir maiúsculas, minúsculas, números, símbolos
#    - Hash em produção (PBKDF2, bcrypt, etc)
#
# 4. CORS:
#    - Em produção, especificar domínios concretos
#    - Não usar "*" em produção
#
# 5. DATABASE:
#    - Manter dados em /data/ (não em Git)
#    - Fazer backup regular
#    - Usar permissões apropriadas (chmod 600)
#
# ============================================================================

# ============================================================================
# VARIÁVEIS OPCIONAIS (Remover # para usar)
# ============================================================================

# Proxy HTTP (se necessário)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080

# Certificado SSL customizado
# SSL_CERT_FILE=/path/to/cert.pem
# SSL_KEY_FILE=/path/to/key.pem

# Diretório temporário
# TEMP_DIR=/tmp/calendarios

# Timezone
# TZ=Europe/Lisbon

# ============================================================================